# 업데이트 로그

## 목차

- [2026-08-08 · 관리자 업무 현황 대시보드](#2026-08-08--관리자-업무-현황-대시보드)
- [2026-08-07 · 직원 매뉴얼 이미지 중심 개편과 FAQ 추가](#2026-08-07--직원-매뉴얼-이미지-중심-개편과-faq-추가)
- [2026-08-06 · Render 실제 배포 완료](#2026-08-06--render-실제-배포-완료)
- [2026-08-05 · Render 배포 기획](#2026-08-05--render-배포-기획)
- [2026-08-05 · 배포 전 구조 점검: 대시보드 접근 모듈 표시 수정 및 완료된 스크립트 정리](#2026-08-05--배포-전-구조-점검-대시보드-접근-모듈-표시-수정-및-완료된-스크립트-정리)
- [2026-08-05 · docs 폴더 정리](#2026-08-05--docs-폴더-정리)
- [2026-08-05 · Seed 직원 3명 재직 상태 정상화](#2026-08-05--seed-직원-3명-재직-상태-정상화)
- [2026-08-05 · 직원 상세 진입 통일 및 프론트엔드 권한 판별 버그 수정](#2026-08-05--직원-상세-진입-통일-및-프론트엔드-권한-판별-버그-수정)
- [2026-08-05 · README·UPDATELOG·ROADMAP 정합화 점검](#2026-08-05--readmeupdatelogroadmap-정합화-점검)
- [2026-08-05 · 채용 포스터 Supabase Storage 전환](#2026-08-05--채용-포스터-supabase-storage-전환)
- [2026-08-05 · ATS 지원자 관리 MVP](#2026-08-05--ats-지원자-관리-mvp)
- [2026-08-04 · 대시보드 실제 업무 데이터 연동](#2026-08-04--대시보드-실제-업무-데이터-연동)
- [2026-08-04 · 근태 변경 이력](#2026-08-04--근태-변경-이력)
- [2026-08-03 · Jira 반복 작업용 CSV 템플릿](#2026-08-03--jira-반복-작업용-csv-템플릿)
- [2026-08-03 · 모바일 직원·조직 관리 화면 보완](#2026-08-03--모바일-직원조직-관리-화면-보완)
- [2026-08-03 · v0.6.4 Playwright E2E 기반 구성](#2026-08-03--v064-playwright-e2e-기반-구성)
- [2026-08-03 · v0.6.3 인증·권한 자동 테스트](#2026-08-03--v063-인증권한-자동-테스트)
- [2026-08-02 · v0.6.2 Supabase Auth 인증 검증](#2026-08-02--v062-supabase-auth-인증-검증)
- [2026-08-01 · v0.6.0 직원·조직 관리 안정화](#2026-08-01--v060-직원조직-관리-안정화)
- [2026-07-31 · v0.5.x 채용·승인 업무 흐름](#2026-07-31--v05x-채용승인-업무-흐름)
- [2026-07-30 · v0.1~v0.4 기반 기능](#2026-07-30--v01v04-기반-기능)

---

## 2026-08-08 · 관리자 업무 현황 대시보드
- 조직도 팝업의 로컬 이미지 쿼리 문자열로 발생하던 Next.js 런타임 오류를 제거하고, 버전 파일명으로 캐시를 갱신하도록 변경했습니다. 팝업 이미지가 실제로 로드되는지 데스크톱·모바일 E2E 검증을 추가했습니다.
- 최신 조직 데이터와 일치하는 조직도 PNG로 교체했습니다. 대표이사·부서별 팀 구성·직원번호·인원 합계를 반영하고, 직원·조직 관리의 조직도 보기 팝업에서 같은 이미지를 표시합니다.
- Playwright E2E를 데스크톱 Chromium과 모바일 Chromium(Pixel 7)에서 순차 실행하도록 보완했습니다. 모바일에서 숨겨지는 연결 상태 표시를 DOM 상태로 검증하고, 반응형 직원 목록 선택자와 승인 가능한 E2E 관리자 직급을 실제 정책에 맞췄습니다.

- 기존 개인 업무 요약 카드와 최근 업무 목록을 유지하면서, `SUPER_ADMIN`·`HR_ADMIN`에만 조직 업무 현황을 추가했습니다.
- 전자결재 상태별 건수와 상신부터 처리까지의 평균 처리 시간, 지원자 전형별 건수와 전체 채용 요청 수, 당일 근태 상태별 건수를 실제 데이터에서 읽어 표시합니다.
- 활성 직원 수에서 당일 근태 등록 인원을 뺀 근태 미등록 수를 추가해, 관리자 화면에서 바로 확인하고 직원·근태 관리 화면으로 이동할 수 있게 했습니다.
- 화면은 상단 4개 핵심 KPI, 결재 상태 도넛, 실제 지원자 전형 흐름, 근태 경고 카드 중심으로 재구성했습니다. 데이터가 없는 항목은 임의 수치나 Mock 데이터 대신 안내 문구를 표시합니다. 일반 직원과 팀 관리자는 조직 전체 집계값을 받지 않습니다.
- 집계 조회는 Repository, 조합과 권한 분기는 DashboardService에 두고 Router는 기존 조회 의존성만 유지했습니다.
- 전자결재의 결재자 선택과 서버 검증을 팀장급 이상(직급명에 팀장·부장·이사·대표 포함)으로 제한했습니다. 채용 요청도 같은 프론트엔드 정책을 재사용합니다.
- `TEAM_ADMIN` 승인 권한 확인이 API 응답 객체의 없는 `team_id`를 읽어 500을 내던 문제를 DB 모델 조회로 수정했습니다. `SUPER_ADMIN` 타인 문서 승인, `HR_ADMIN` 지정 문서 승인, `TEAM_ADMIN` 같은 팀 승인·지정 문서 반려를 자동 테스트로 추가했습니다.
- 조직을 개발팀(SW·HW·QA파트)과 CS팀으로 정리했습니다. 기존 독립 QA팀의 5명은 CS팀으로 전환하고, 개발팀 QA파트에 파트장 1명과 QA 인력 2명을 추가했습니다.
- 파트가 없는 개발팀장은 개발팀 전체를, 파트장은 자기 파트만 직원·조직 및 근태 관리 범위로 조회·관리하도록 권한 범위를 보완했습니다. CS팀의 모바일 조직 배지도 별도 색상으로 표시합니다.
- `20260808_0017` Alembic migration을 Supabase PostgreSQL에 적용해 기존 QA팀 명칭·업무와 새 QA파트 인력을 실제 데이터에 반영했습니다.
- 마케팅·기획·CS 부서장도 각자 소속 부서만 조회되는 권한 테스트를 추가했습니다. QA파트 3명은 기존 계정에 영향을 주지 않는 전용 provisioning 절차로 Supabase Auth와 연결했으며, 최다은(파트장)에게 `TEAM_ADMIN`, 정하윤·유민재에게 `EMPLOYEE` 권한을 부여했습니다.
- 상단 현재 사용자 영역은 역할·직급 표기를 제거하고 모든 사용자에게 이름만 표시하도록 통일했습니다.
- 대표이사는 화면과 조직도에서 부서·팀을 `-`로 표시하고, 경영진 부서는 부서 목록에서 숨겼습니다. 마케팅·인사·기획은 1팀·2팀, CS는 CS1팀으로 재편했으며 조직도는 고정 이미지 대신 실제 조직 데이터를 조회합니다.
- QA파트 3명은 `MS0012`~`MS0014`로 재배치하고, 기존 마케팅팀부터 CS팀까지 사번을 순차적으로 3번씩 뒤로 이동하도록 migration을 추가했습니다. 로그인 계정·결재·근태 이력은 사번이 아닌 내부 ID를 유지합니다.
- 개발 하위 조직 명칭을 SW개발팀·HW개발팀·QA팀으로 통일하고, 매뉴얼의 「조직도 확인 방법」 이미지를 새 조직 구조를 반영한 SVG로 교체했습니다. 조직도 응답에는 팀명을 포함해 실제 팀 소속을 표시합니다.
- 조직도 자산을 새 PNG로 교체하고, 직원·조직 관리의 「조직도 보기」는 API 조회 대신 같은 이미지를 직접 표시하도록 바꿔 조회 오류 없이 열리게 했습니다.

### 검증 결과

- Backend: Ruff 검사·포맷 검사 통과, pytest 84개 통과, Alembic head(`20260808_0017`) 적용 확인
- 운영 계정: QA파트 3명 로그인 계정과 `employee_accounts` 역할·활성 상태 확인
- Frontend: lint, TypeScript 검사, production build 통과

---

## 2026-08-07 · 직원 매뉴얼 이미지 중심 개편과 FAQ 추가

### 직원 매뉴얼 UI 단순화

- 매뉴얼 카드를 이미지 중심으로 재구성하고 카드에서 중요 배지, 카테고리 배지, 대상 역할, 최근 수정일을 제거했습니다. 이제 카드에는 큰 설명 이미지, 제목, 간략 설명만 표시됩니다.
- 카드 대표 이미지를 임시 요약 SVG에서 **각 매뉴얼이 설명하는 실제 앱 화면 캡처**로 교체했습니다. 처음에는 직원 이용 가이드 PDF의 페이지 이미지를 썼지만, PDF는 기능 영역 단위 개요라 "근무 상태 등록"인데 직원 목록이 보이는 식으로 카드 주제와 어긋났습니다. 그래서 Playwright로 로그인 화면, 대시보드, 직원 검색, 근무 상태 변경 모달, 결재 작성 폼, 결재 상세(처리 이력 포함), 채용 요청 상세, 지원자 관리 8개 화면을 직접 캡처해 사용합니다. 조직도는 기존 실제 이미지를 그대로 씁니다.
- 캡처는 모두 동일한 1440×900 뷰포트로 찍어 카드 높이가 들쭉날쭉해지지 않도록 했고, 개발용 Seed 관리자 계정으로 로그인해 실제 업무 데이터가 담긴 화면을 담았습니다. 이미지는 카드 폭을 꽉 채우되 `object-fit: contain`으로 비율을 유지합니다.
- 매뉴얼 상세 페이지(`/manuals/[slug]`) 진입을 제거했습니다. 카드 이미지를 클릭하면 상세 페이지 대신 확대 보기 모달이 열리며 ESC 또는 바깥 영역 클릭으로 닫힙니다.
- 상세 페이지에만 있던 매뉴얼 삭제 기능을 관리자 편집 화면으로 옮겨 `SUPER_ADMIN`, `HR_ADMIN`의 작성·수정·삭제 권한을 그대로 유지했습니다. 카드의 편집 아이콘도 관리자에게만 표시됩니다.
- 목록 상단에 직원 이용 가이드 PDF 다운로드 버튼을 추가했습니다. PDF 경로는 `features/manuals/constants.ts` 한 곳에서 관리합니다.
- 검색과 카테고리 필터는 그대로 유지하고, 중요 문서 상단 고정 정렬을 제거해 모든 카드가 동일한 시각적 비중을 갖도록 했습니다.
- UI에서 사용하지 않게 된 `manuals.content`, `slug`, `target_roles`는 관리자 편집과 향후 RAG 활용을 위해 DB에 그대로 보존했습니다. migration으로 컬럼을 삭제하지 않았습니다.

### FAQ 신규 기능

- 왼쪽 사이드바에 독립 메뉴 「FAQ」와 `/faq` 화면을 추가했습니다. 질문을 클릭하면 답변이 펼쳐지는 Accordion 방식이며, 카드 박스 없이 넓은 여백과 구분선만 사용합니다.
- FAQ를 컴포넌트에 하드코딩하지 않고 `manual_faqs` 테이블과 `20260807_0016` migration을 추가해 Backend에서 관리합니다. `related_manual_id`로 기존 매뉴얼을 선택적으로 참조할 수 있고, 매뉴얼이 삭제돼도 FAQ는 `SET NULL`로 남습니다.
- `GET /api/v1/faqs`는 인증된 모든 역할(`SUPER_ADMIN`, `HR_ADMIN`, `TEAM_ADMIN`, `EMPLOYEE`)이 조회할 수 있고 비로그인 접근은 차단됩니다. 공개 상태(`is_published`) FAQ만 반환합니다.
- 로그인·계정, 직원·조직, 근태·휴가, 전자결재, 채용 요청·ATS, 권한 6개 분류에 걸쳐 초기 FAQ 18개를 Seed로 추가했습니다. 고정 id 기준으로 갱신하므로 반복 실행해도 중복 생성되지 않습니다.
- 모바일 하단 탭 네비게이션이 5칸 고정이라 FAQ가 잘리던 문제를 6칸으로 조정했습니다.

### 매뉴얼 15개 → 9개 정리

- 카드 15개가 이미지 6종을 나눠 쓰면서 같은 그림이 최대 5번(전자결재) 반복되던 문제를 해결했습니다. 직원 이용 가이드 PDF의 기능 구조에 맞춰 9개 핵심 매뉴얼로 통합했고, 이후 각 카드가 자기 주제의 실제 화면을 갖도록 이미지를 개별 캡처해 **중복 이미지가 완전히 사라졌습니다**.
- 통합 내역: 로그인·비밀번호·세션(3→1), 근무 상태·병가 사유(2→1), 전자결재 작성·상신(2→1)과 승인·반려·이력·권한(3→1), 채용 요청·포스터·상신·공고 확인(4→1). 여기에 그동안 비어 있던 업무 홈 대시보드와 직원 검색 매뉴얼을 복원하고 지원자 전형 단계 관리를 추가했습니다. 조직도 확인 방법은 그대로 유지했습니다.
- 통합되는 매뉴얼의 본문은 버리지 않고 핵심만 추려 남는 매뉴얼의 `content`에 소제목과 함께 합쳤습니다. 화면에는 노출되지 않지만 향후 RAG가 검색할 문장은 유지됩니다.
- 매뉴얼이 없어 필터에서 0건이 나오던 "역할별 권한/보안" 카테고리를 제거해 카테고리 7개 → 6개가 되었고, 이제 모든 카테고리에 실제 매뉴얼이 있습니다. 권한 관련 안내는 FAQ의 권한 분류 3건이 담당합니다.
- `seed_manuals`에 빈 카테고리 정리 로직을 추가했습니다. 소속 매뉴얼이 없을 때만 삭제하므로 외래키 제약과 충돌하지 않습니다. 테이블·컬럼 삭제나 신규 migration은 없으며 seed 데이터만 변경했습니다.

### FAQ 카테고리 그룹핑

- FAQ 18개가 하나의 긴 목록으로만 나열되던 것을 이미 있던 `category` 값(로그인·계정, 직원·조직, 근태·휴가, 전자결재, 채용 요청·ATS, 권한 6개) 기준으로 묶어 그룹 제목과 함께 보여주도록 했습니다. 그룹 순서와 그룹 내 질문 순서는 `display_order`를 그대로 따릅니다.
- 상단에 "전체" + 카테고리 6개 필터 버튼을 추가했습니다. 특정 카테고리를 선택하면 그 카테고리의 질문만 남고 나머지는 화면에서 사라지며, 선택된 버튼은 파란색으로 표시됩니다. "전체"를 다시 누르면 6개 그룹이 제목과 함께 전부 보이는 상태로 돌아갑니다. 카드 박스나 배경 없이 기존의 넓은 여백·구분선 스타일은 그대로 유지했습니다.
- 백엔드 변경은 없습니다. 프론트엔드에서 이미 받아온 목록을 그룹화만 했습니다.

### 함께 수정한 것

- Playwright E2E 4개가 실패해 원인을 확인한 결과, Next.js 개발 모드 인디케이터가 사이드바 좌측 하단의 설정 버튼을 덮어 클릭을 가로막고 있었습니다. FAQ 메뉴를 제거한 상태에서도 동일하게 실패하는 것을 확인해 이번 변경과 무관한 개발 환경 문제임을 확인했고, `next.config.ts`에 `devIndicators: false`를 추가해 해결했습니다.

### 검증 결과

- Backend: pytest 73개(FAQ 4개 추가), Ruff 검사·포맷 검사 통과
- Frontend: lint, TypeScript 검사, production build 통과
- E2E: 기존 Playwright 6개 전부 통과
- 화면: PC·모바일(402px)에서 매뉴얼 목록, 이미지 확대 보기, FAQ 펼침, 모바일 하단 탭의 FAQ 이동을 실제 브라우저로 확인
- DB: Supabase에 `20260807_0016` 적용, FAQ Seed 2회 실행 후에도 18건 유지 확인. 매뉴얼 Seed 2회 실행 후 카테고리 6개·매뉴얼 9개 유지, 빈 카테고리 0개·고아 asset 0개 확인
- 화면: 정리 후 매뉴얼 카드가 정확히 9개로 3×3 배치되고 카테고리 필터 6개가 모두 실제 결과를 반환하는 것을 브라우저로 확인

---

## 2026-08-06 · Render 실제 배포 완료

- 전날 세운 배포 기획대로 프론트엔드·백엔드를 Render에 실제로 배포했습니다.
  - 백엔드(FastAPI): https://ms-flowhub.onrender.com (Root `backend`, `uvicorn app.main:app --host 0.0.0.0 --port $PORT`)
  - 프론트엔드(Next.js): https://ms-flowhub-frontend.onrender.com (Root `frontend`, `npm run start -- -p $PORT`)
- 배포 순서대로 단계별 검증을 거쳤습니다.
  - 백엔드 `/health`가 Supabase DB 연결까지 확인된 상태로 응답하는 것을 원격으로 확인했습니다.
  - 백엔드 `FRONTEND_ORIGIN`을 실제 프론트엔드 URL로 갱신해 CORS(`access-control-allow-origin`, `allow-credentials`)가 정상 동작함을 확인했습니다.
  - 프론트엔드의 `/api/*` Next.js rewrite 프록시가 실제로 백엔드까지 연결되어 Supabase 데이터를 반환하는 것을 확인했습니다.
  - Supabase Auth의 Site URL·Redirect URL을 프론트엔드 배포 도메인으로 등록했습니다.
- 배포 후 실제 사용자 스모크 테스트(로그인, 대시보드, 직원·조직 관리, 전자결재 작성·상신, 채용 요청·포스터 첨부·ATS 지원자 등록, 직원 매뉴얼 조회)를 전부 통과했습니다.
- Render 무료 플랜 특성상 유휴 시 슬립 후 첫 요청에 콜드스타트(수십 초)가 발생할 수 있습니다. 배포 실패가 아닌 정상 동작입니다.

### 검증 결과

- 배포 전 최종 로컬 점검: Backend pytest 69개·Ruff 통과, Frontend lint·typecheck·production build 통과
- 배포 후: 백엔드 `/health` 200, 프론트엔드 주요 페이지 7종 200, 인증 필요 API 프록시 401(정상), CORS 헤더 정상
- 사용자 수동 스모크 테스트 전 항목 통과

---

## 2026-08-05 · Render 배포 기획

- 내일(2026-08-06) 배포를 앞두고 프론트엔드·백엔드를 어디에 올릴지 정리했습니다. 현재 백엔드가 요청마다 동기 SQLAlchemy 세션으로 Supabase에 직접 연결하는 상시 서버 구조라 Vercel 서버리스 Python 함수와는 맞지 않아, 프론트엔드·백엔드 모두 Render Web Service로 배포하는 방향으로 정했습니다.
- 배포 구조, 서비스별 빌드/시작 명령, 필요한 환경변수, 배포 순서, 배포 후 스모크 테스트 항목을 `docs/DEPLOYMENT_PLAN.md`에 정리했습니다.
- 채용 포스터를 Supabase Storage로 이미 옮겨둔 것이 배포 리스크(재배포 시 로컬 파일 소실)를 사전에 없앤 상태라는 점을 문서에 명시했습니다.
- Jira 현황 CSV를 확인해 이미 완료됐는데 "진행 중"으로 남아있던 항목(`MFH-26` ATS 지원자 관리 MVP, `MFH-27` Supabase Storage 전환, `MFH-23` 직원 매뉴얼 MVP 검증, `MFH-22` 프로젝트 문서 기준점 정리)을 완료로 갱신하고, Jira에 없던 오늘 작업(RBAC 권한 판별 오류 수정, 직원 상세 UX 통일·Seed 정상화, docs 폴더 정리, 배포 전 구조 점검)과 내일 배포 체크리스트(Render 백엔드·프론트엔드 배포, 배포 후 점검)를 새 항목으로 추가한 재가져오기용 CSV를 만들었습니다. 실제 Jira 반영은 사용자가 CSV를 가져오기(import)하는 방식으로 진행합니다.

---

## 2026-08-05 · 배포 전 구조 점검: 대시보드 접근 모듈 표시 수정 및 완료된 스크립트 정리

- 내일 배포 기획 전 전체 백엔드·프론트엔드 소스의 import 관계를 전수 대조해 구조를 점검했습니다. 참조되지 않는 죽은 파일은 없었습니다.
- 대시보드 "접근 가능 모듈" 카드(`GET /api/v1/dashboard`의 `accessible_modules`)가 실제 RBAC 권한(`employee_accounts.role`)이 아니라 레거시 직무 분류 컬럼(`employees.role`) 기준으로 계산되어, 예를 들어 실제 `HR_ADMIN`인데도 "직원·조직 관리"·"직원 매뉴얼" 모듈이 목록에서 빠지는 등 표시 내용이 부정확했습니다. `DashboardService.get_dashboard`가 인증된 사용자의 RBAC 역할을 받아 `SUPER_ADMIN`/`HR_ADMIN`/`TEAM_ADMIN`/`EMPLOYEE` 기준으로 정확한 모듈 목록을 반환하도록 수정했습니다.
- 이미 대상 데이터가 모두 삭제되어 실행해도 아무 일도 하지 않는 1회성 정리 스크립트 `app/scripts/remove_legacy_test_employees.py`를 삭제했습니다.

### 검증 결과

- Backend: pytest 69개, Ruff 검사·포맷 검사 통과
- Frontend: production build 통과(백엔드 응답 형태는 변경 없음)

---

## 2026-08-05 · docs 폴더 정리

- `docs/`에 12개 마크다운 파일이 쌓여 있어, 각 문서가 스스로 명시한 상태 문구를 기준으로 현재 유효한 문서와 과거 스냅샷·착수 전 기획 문서를 분리했습니다.
- 다음 4개 파일을 `docs/archive/`로 옮겼습니다. 모두 이미 실제 구현 문서(README, UPDATELOG, API_SPEC, DATA_MODEL)로 대체된 상태였습니다.
  - `IMPLEMENTATION_SUMMARY.md`: 2026-07-31 구현 스냅샷, UPDATELOG로 대체됨
  - `PROTOTYPE_CHECKLIST.md`: v0.5.0 시점 체크리스트, 이미 완료된 항목이 미착수로 남아 있어 오히려 혼동을 줌
  - `USER_FLOWS.md`: 존재하지 않는 API를 참조하는 미래 설계 초안
  - `ATS_APPLICANT_MVP_PLAN.md`: ATS 지원자 관리 착수 전 기획 문서, 해당 기능은 이미 구현·검증 완료
- `docs/`에는 현재도 실시간으로 갱신하는 문서(API_SPEC, DATA_MODEL, ROADMAP, PROJECT_SPEC)와 누적 기록 성격의 문서(DECISIONS, AI_DESIGN)만 남았습니다.
- README의 "관련 문서" 목록과 `docs/ROADMAP.md`의 참조 링크를 새 위치에 맞게 갱신했습니다.
- `docs/LEARNING_LOG.md`는 2026-07-31 항목 이후 실제로 쓰이지 않아 삭제했습니다. AGENTS.md의 Learning Rules는 특정 파일을 지정하지 않으므로 함께 수정할 내용은 없습니다.

---

## 2026-08-05 · Seed 직원 3명 재직 상태 정상화

- 조직 Seed(`seed_organization`)가 매 실행마다 `문태호(MS0018)`를 휴직, `류하늘(MS0031)`을 입사 예정, `윤가은(MS0041)`을 퇴직 상태로 강제로 되돌리던 하드코딩을 제거했습니다. 이 3명은 실제로 퇴사·휴직한 것이 아니라 재직 상태별 화면을 보여주기 위한 샘플이었고, 그 때문에 이 3명만 직원 상세 화면이 다른 직원과 다르게 보였습니다.
- Seed를 다시 실행해 실제 Supabase 데이터에서도 46명 전원이 재직(`ACTIVE`)·활성(`is_active=true`) 상태이고 재직 상태 사유가 비어 있는 것을 확인했습니다. 이제 세 직원도 다른 직원과 동일하게 "근무 상태 변경"·"변경 이력" 버튼이 나란히 보이는 상세 화면을 사용합니다.
- 휴직·입사 예정·퇴직 상태 자체(휴직 사유 패널·폼 등)는 기능으로는 그대로 남아 있으며, 필요하면 직원 상세에서 재직 상태를 다시 그 값으로 바꿔 확인할 수 있습니다.

### 검증 결과

- Backend: pytest 69개, Ruff 검사·포맷 검사 통과
- DB: Supabase PostgreSQL에서 3명 포함 전체 46명의 `employment_status=ACTIVE`, `is_active=true` 직접 조회로 확인

---

## 2026-08-05 · 직원 상세 진입 통일 및 프론트엔드 권한 판별 버그 수정

- 직원·조직 관리 목록에서 재직/근무 상태 배지 옆에 있던 별도 "ⓘ" 사유 보기 버튼을 제거하고, 이름을 클릭하면 열리는 직원 상세로 진입 경로를 통일했습니다. 두 버튼 모두 같은 상세 화면을 열었던 중복 기능이었습니다.
- 직원·조직 관리 점검 중 프론트엔드 전반의 권한 판별이 잘못된 값을 기준으로 동작하던 것을 발견해 함께 고쳤습니다.
  - 로그인한 사용자의 `currentEmployee.role`이 실제 RBAC 권한(`employee_accounts.role`, 예: `SUPER_ADMIN`/`HR_ADMIN`/`TEAM_ADMIN`)이 아니라, 직원 목록 조회 API가 내려주는 레거시 직무 분류 컬럼(`employees.role`, 예: `DEPARTMENT_HEAD`/`HR_MANAGER`/`ADMIN`)을 그대로 쓰고 있었습니다.
  - 그 결과 백엔드는 이미 `HR_ADMIN`, `TEAM_ADMIN` 권한을 정상적으로 허용하는데도, 프론트엔드는 두 역할을 인식하지 못해 매뉴얼 작성, 지원자 등록·수정, 근무 상태 변경 같은 버튼이 보이지 않는 화면별 오동작이 있었습니다. `SUPER_ADMIN` 계정(김민성)만 레거시 컬럼 값이 우연히 `ADMIN`이어서 일부 화면이 정상처럼 보였습니다.
  - 백엔드 `/api/v1/auth/me`는 처음부터 정확한 RBAC 권한을 내려주고 있었으나 프론트엔드가 `employee_id`만 사용하고 버려지고 있었습니다. 이제 로그인 시 이 값을 받아 목록에서 조회한 본인 항목의 `role`을 교체하는 방식으로 고쳤습니다. 다른 직원의 `role_label` 표시(직무 분류 라벨)는 그대로 유지됩니다.
  - 이 수정으로 정확해진 `currentEmployee.role`에 맞춰, 여러 화면에 남아 있던 죽은 레거시 역할 문자열(`"ADMIN"` 등)도 함께 정리했습니다(직원 상세 편집, 지원자 관리, 매뉴얼 관리, 전자결재 자기 결재 예외·전체 부서 기안, 채용 요청 삭제).

- 직원 목록 API(`/employee-options`) 응답에 `team_code`를 추가해, `TEAM_ADMIN`이 다른 팀 직원 상세를 열었을 때 "근무 상태 변경" 버튼이 잘못 표시되던 문제를 마저 고쳤습니다. 이제 로그인한 `TEAM_ADMIN`의 팀과 선택한 직원의 팀이 같을 때만 버튼이 보입니다. 백엔드 권한 검증(같은 팀만 허용)은 그대로 유지됩니다.

### 검증 결과

- Backend: pytest 69개, Ruff 검사·포맷 검사 통과
- Frontend: lint, TypeScript 검사, production build 통과
- Playwright E2E 6개 전부 통과(로그인, 세션 유지, 로그아웃, 권한 범위, 직원 필터, SUPER_ADMIN 전자결재 승인, 비밀번호 변경 복구)

---

## 2026-08-05 · README·UPDATELOG·ROADMAP 정합화 점검

- 오늘 완료한 작업이 문서와 실제 상태에 맞게 반영됐는지 점검했습니다.
- `docs/ROADMAP.md`가 이미 완료된 "Supabase Storage 전환"을 여전히 미구현·다음 작업으로 표시하고 있어 완료 항목으로 옮기고, CI/CD·운영 배포 기본 구성만 다음 작업으로 남겼습니다.
- `TROUBLESHOOTING.md`에 Supabase Storage Object API가 없음 상태를 HTTP 404가 아니라 400 + 응답 본문으로 표현하는 문제와 대응 방법을 추가했습니다.
- 남은 확인 필요 사항: `backend/pyproject.toml`(0.5.11)과 `frontend/package.json`(0.5.10) 버전 번호가 UPDATELOG의 `v0.7.x` 마일스톤 표기와 맞지 않고, ATS·Storage 전환 관련 Playwright E2E 시나리오가 아직 없습니다(현재는 Backend pytest와 수동 검증만 완료).

---

## 2026-08-05 · 채용 포스터 Supabase Storage 전환

- 채용 포스터 파일을 백엔드 로컬 디스크(`backend/data/uploads/recruitment-posters/`) 대신 Supabase Storage 비공개 버킷(`recruitment-posters`)에 저장하도록 전환했습니다.
- 새 SDK 의존성을 추가하지 않고, 기존 Supabase Auth 연동과 동일한 `urllib` 기반 REST 호출 패턴으로 `app/core/supabase_storage.py`를 추가했습니다.
- 업로드·다운로드·삭제 모두 백엔드 API를 거치며, 버킷은 비공개로 유지해 권한 없는 사용자가 Storage URL을 직접 열람할 수 없습니다. 다운로드 시 기존과 동일하게 요청 작성자·결재자·관리자 권한을 서버에서 검증합니다.
- 버킷을 처음 준비하는 `app/scripts/setup_storage_bucket.py`와, 기존에 로컬에 저장돼 있던 포스터를 Storage로 옮기는 `app/scripts/migrate_posters_to_storage.py`를 추가했습니다. 실제 운영 DB에서 포스터가 연결된 요청은 1건이었고, 해당 파일을 Storage로 이전한 뒤 바이트 단위로 다운로드 결과가 일치하는 것을 확인했습니다.
- 더 이상 사용하지 않는 로컬 저장 모듈(`app/core/upload_storage.py`)을 제거했습니다.
- pytest가 매 실행마다 실제 Supabase Storage에 접근하지 않도록, DB를 SQLite로 대체하는 기존 방식과 동일하게 Storage 호출을 인메모리 가짜 저장소로 대체하는 `fake_poster_storage` autouse fixture를 추가했습니다.

### 검증 결과

- Backend: pytest 69개, Ruff 검사·포맷 검사 통과
- 실제 Supabase Storage에 대해 버킷 생성, 기존 포스터 업로드, 서비스 계층을 통한 다운로드까지 수동으로 왕복 확인(바이트 수 일치)

---

## 2026-08-05 · ATS 지원자 관리 MVP

- 외부 공개 지원 페이지 없이 HR이 지원자를 수동 등록하고, 채용공고별 지원 단계와 이력을 관리하는 MVP를 구현했습니다.
- `applicants`, `applicant_stage_histories` 테이블과 `20260805_0015` Alembic migration을 추가하고 Supabase PostgreSQL에 적용했습니다.
- 목록 검색·공고/단계 필터, 상세 이력, 지원자 등록·삭제, 단계 변경 화면을 추가했습니다.
- `SUPER_ADMIN`, `HR_ADMIN`은 등록·수정·삭제·단계 변경을 수행하고, `TEAM_ADMIN`은 본인 부서 공고만 조회하며, `EMPLOYEE`는 지원자 API를 조회할 수 없도록 서버에서 검증합니다.
- 같은 공고의 이메일 중복, 불합격 사유 미입력, 종료 단계 재변경을 API 규칙으로 차단했습니다.
- 이력서 파일, 외부 공개 지원 화면, 이메일 발송·면접 일정·AI 자동 평가는 이번 MVP에서 제외하고 이후 확장으로 유지했습니다.
- 이미 구현되어 있던 지원자 정보 수정(PATCH) API를 지원자 상세 화면에 연결해, 등록 담당자가 오탈자를 등록 후에도 이름·이메일·전화번호·경력 요약을 직접 고칠 수 있도록 "수정" 버튼과 수정 폼을 추가했습니다.
- 전형 단계 이력이 3건을 넘어가면 이력 영역만 스크롤되도록 조정했습니다.
- 전형 단계 변경 메모 입력창이 다른 입력 필드와 다른 글꼴 크기로 표시되어 안내 문구가 두 줄로 줄바꿈되던 문제를 다른 폼 요소와 동일한 스타일로 맞춰 한 줄로 보이도록 수정했습니다.
- "수정", "삭제" 버튼 사이 간격을 추가했습니다.
- 전형 단계 표시명을 `INTERVIEW` "면접" → "1차 면접", `OFFERED` "최종 제안" → "2차 면접"으로 변경했습니다. DB에 저장되는 단계 값은 그대로 유지해 기존 이력과 호환됩니다.
- Supabase PostgreSQL에 실제로 저장된 지원자·이력 데이터를 직접 조회해 화면 등록 내용과 DB 데이터가 일치하는 것을 확인했습니다.

### 검증 결과

- Backend: pytest 69개, Ruff 검사·포맷 검사 통과
- Frontend: lint, TypeScript 검사, production build 통과
- DB: Supabase PostgreSQL Alembic head `20260805_0015` 확인, `applicants`/`applicant_stage_histories` 데이터 직접 조회로 실연동 확인

---

## 2026-08-04 · 근태 변경 이력

- 근무 상태가 실제로 변경된 경우에만 이전·이후 상태, 사유, 변경자, 변경 시각을 저장하는 `attendance_change_histories` 테이블과 Alembic migration을 추가했습니다.
- 직원 상세 모달에서 선택 날짜의 근태 변경 이력을 최신순으로 확인하도록 추가했습니다. 직원 목록은 현재 상태만 유지합니다.
- 비공개 사유는 기존 근태 사유 정책과 동일하게 관리자·인사담당자만 조회하며, 일반 직원과 팀 관리자의 이력 응답에서는 제외합니다.
- 신규 일자 근태와 이력을 함께 저장할 때 PostgreSQL 외래키가 이력을 먼저 저장하던 순서 오류를 수정했습니다.
- 직원 이름 클릭 시 열리는 상세 화면을 데스크톱 2열(직원·근태 / 변경 이력)로 재구성하고, 중복되던 재직·근무 요약 행을 제거했습니다. 모바일에서는 한 열로 표시됩니다.
- 직원 상세에서 담당 역할 문구를 제거하고, 근무 상태 변경 폼과 근태 변경 이력을 각각 버튼으로 열고 닫을 수 있도록 보완했습니다. 닫기 아이콘은 제목 영역으로 옮겨 이력 카드와 겹치지 않게 했습니다.
- 근무 상태 상세 패널을 제거하고, 근태 변경 이력은 네 건을 기준으로 목록 영역만 스크롤되도록 조정했습니다. 상세·이력 카드 묶음은 화면 중앙에 정렬됩니다.
- 근무 상태 변경 폼의 사유 분류 드롭다운과 비공개 상세 입력을 제거하고, 근무 상태 선택에 `기타` 상태를 추가했습니다.
- 근무 상태 변경 또는 휴직 사유 입력 화면이 열린 상태에서 바깥 영역이나 닫기 아이콘을 누르면, 작성 중인 내용이 사라진다는 확인 후에만 직원 상세를 닫도록 변경했습니다.
- 실제 완료 항목과 다음 우선순위를 Jira 보드·타임라인에 옮길 수 있도록 로드맵에 2026-08-04 완료 작업과 2026-08-05~07 예정 작업을 정리했습니다.

### 검증 결과

- Backend: 근태 변경 이력 생성·중복 저장 방지·비공개 사유 마스킹 API 테스트를 추가했습니다.
- Frontend: lint와 production build를 통과했습니다.

---

## 2026-08-04 · 대시보드 실제 업무 데이터 연동

- 대시보드의 고정값 카드(승인 필요 견적, 읽지 않은 알림)를 제거하고, 현재 사용자의 결재 대기·상신 결재와 실제 채용공고 수를 표시하도록 변경했습니다.
- 최근 업무 목록은 안내용 ATS·CRM 항목 대신 현재 사용자와 관련된 전자결재 및 채용 요청 데이터를 표시하도록 변경했습니다.
- CRM Lite 접근 모듈과 고정 날짜 표기를 제거했으며, 대시보드 데이터 조회 실패 시 안내용 mock 데이터를 대신 표시하지 않도록 정리했습니다.
- 값이 1 이상인 업무 지표 카드는 해당 전자결재 또는 채용공고 화면으로 이동하도록 연결했습니다.
- 상단 알림 종 아이콘은 사내 메일 시스템 도입 전까지 비활성 상태로 전환하고, 고정 알림 개수 표시는 제거했습니다.
- 관리자가 채용 요청에 연결된 전자결재를 삭제할 때 관련 채용 요청·공고·알림도 함께 정리하도록 보완해 FK 제약으로 발생하던 삭제 요청 실패를 해결했습니다.
- 김민성 `SUPER_ADMIN` 계정은 채용 요청에서 본인을 결재자로 선택하고 해당 요청을 처리할 수 있도록 예외를 추가했습니다. 일반 직원의 자기 결재 금지 규칙은 유지됩니다.
- 채용 요청 자기 결재 예외와 일치하도록 전자결재 상세 화면에서도 김민성 관리자에게 승인·반려 버튼을 표시하도록 보완했습니다.
- README·API 명세·데이터 모델·로드맵을 현재 구현 기준으로 정리하고, CRM·견적·AI·알림 조회 등 미구현 설계 문서는 과거 또는 미래 확장 범위로 명시했습니다.
- 사내 운영 플랫폼 범위와 맞지 않는 CRM·고객·견적·영업 기획과 API·데이터 모델 초안을 문서에서 제거했습니다. 과거 수행 기록은 유지했습니다.

### 검증 결과

- Backend: Ruff 검사·포맷 검사 통과, pytest 61개 통과
- Frontend: lint, TypeScript 검사, production build 통과

---

## 2026-08-03 · Jira 반복 작업용 CSV 템플릿

- 기존 `MFH-1`부터 `MFH-6`까지의 에픽에 연결할 19개 하위 작업을 Jira CSV로 구성했습니다.
- Jira에서 내보낸 에픽 일정(2026-08-04~2026-08-15)에 맞춰 각 하위 작업의 시작일·종료일을 배정했습니다.
- 새 Jira CSV 가져오기 방식에 맞춰 기존 에픽 키를 `Parent`로 사용하는 전용 파일을 추가했습니다.
- `Parent` 매핑에 필요한 19개 하위 작업의 고유 `Work item ID`를 추가했습니다.
- Jira 가져오기 로그에서 대상 스페이스의 `Task`·`To Do` 값 누락을 확인해 CSV 값을 `작업`·`할 일`로 맞췄습니다.
- Jira 외부 가져오기의 업무 유형·상태 충돌을 피하기 위해, 에픽별 라벨로 구분한 표준 CSV 가져오기용 최종 파일을 추가했습니다.

---

## 2026-08-03 · 모바일 직원·조직 관리 화면 보완

- 8단계 미사용 코드 정리로 프론트엔드에서 사용하지 않는 조직도 API 클라이언트·타입과 백엔드의 미사용 상태 권한 보조 메서드를 제거했습니다.
- 8단계 Enum·Type 정리로 mock 대시보드 제거 후 남아 있던 프론트엔드 대시보드 source 타입을 백엔드 응답 범위와 일치시켰습니다.
- 8단계 인증·세션 정리로 과거 사용자 전환 의미의 상태명을 인증된 직원 동기화 의미로 바꾸고, 사용되지 않는 인증 보조 함수를 제거했습니다.
- 8단계 프론트엔드 컴포넌트 분리로 전자결재 상세의 상신·승인·반려 처리 패널을 전용 컴포넌트로 분리했습니다.
- 8단계 프론트엔드 컴포넌트 분리로 전자결재 상세의 결재 처리 이력 패널을 전용 컴포넌트로 분리했습니다.
- 8단계 프론트엔드 컴포넌트 분리를 시작해 대시보드의 지표 표시 영역을 전용 컴포넌트로 분리했습니다.
- 8단계 구조 개선을 시작해, 실제 인증 이후에만 직원 선택 정보를 불러오도록 전환하고 대시보드·채용 요청의 안내용 mock/fallback 데이터를 제거했습니다.
- 직원·조직 관리의 검색·필터·조직도 버튼이 중간 화면 폭에서는 한 줄을 유지하고, 모바일에서는 기존 높이의 세로 필터를 유지하도록 너비 규칙을 보완했습니다.
- 대시보드에서 현재 범위만 안내하던 마일스톤 카드를 제거했습니다.
- 조직도에 표시된 개발·마케팅·인사·기획·QA·경영진 색상을 모바일 팀 배지에도 동일한 계열로 적용했습니다.
- 모바일 직원 목록을 `직원·직급·재직 상태·근무 상태` 네 열로 재구성하고, 팀 약어 배지와 상태 중심 표시를 추가했습니다. PC 목록의 기존 전체 정보는 유지됩니다.
- 모바일 조직도 모달은 원본을 축소하지 않고 가로로 이동하며 크게 확인할 수 있도록 보완했습니다.
- README를 현재 구현 상태 중심으로 재구성하고, 별도 `TROUBLESHOOTING.md`를 추가했습니다.
- 오류 원인과 해결 결과는 [문제 해결 문서](./TROUBLESHOOTING.md)를 참고하세요.

---

## 2026-08-03 · v0.7.0 직원 매뉴얼 MVP

### 핵심 기능

- 직원 매뉴얼 카테고리, 텍스트 본문, 이미지/PDF URL 자산 테이블과 Alembic migration을 추가했습니다.
- 목록 검색·카테고리 필터·중요 매뉴얼 상단 고정·최근 수정일, 상세 본문과 이미지 확대 보기를 구현했습니다.
- `SUPER_ADMIN`, `HR_ADMIN`만 카테고리와 매뉴얼을 작성·수정·삭제하고, `TEAM_ADMIN`, `EMPLOYEE`는
  공개 매뉴얼만 조회하도록 백엔드 권한 검증을 추가했습니다.
- 로그인·계정부터 채용 요청까지 7개 카테고리, 15개 초기 매뉴얼과 카테고리별 요약 SVG를 seed로 구성했습니다.
- Supabase PostgreSQL에 `20260803_0013` migration을 적용하고, 초기 매뉴얼 seed를 재실행해도
  중복 오류 없이 처리되는 것을 확인했습니다.
- iPhone 16 Pro 폭(402px)에서는 왼쪽 사이드바를 하단 메뉴로 전환하고, 직원 목록은 직원·부서·직급
  세 열만 표시하도록 반응형 레이아웃을 보완했습니다.
- 모바일 하단 메뉴의 비활성 항목이 숨겨지던 규칙을 제거하고, 다섯 메뉴를 동일한 폭의 탭으로
  항상 표시하도록 보완했습니다.
- `조직도 확인 방법` 매뉴얼은 임시 요약 SVG 대신 직원·조직 관리 화면의 실제 `organization-chart.png`를
  사용하도록 갱신했습니다.
- Playwright 전용 테스트 직원과 비활성 인증 테스트 계정을 정리하는 스크립트를 추가하고, 실제 테스트
  계정 데이터를 제거했습니다. E2E 실행 전에는 전용 seed로 다시 생성합니다.
- Playwright `globalSetup`과 `globalTeardown`으로 E2E 테스트 시작 전 계정을 생성하고 종료 후 자동
  정리하도록 구성했습니다.

### 검증 결과

- 매뉴얼 API 테스트 5개 통과: 목록·검색·필터·상세, 초안 비노출, HR 수정·삭제, 조회 역할 차단, seed 중복 방지.

## 2026-08-03 · v0.6.4 Playwright E2E 기반 구성

### 핵심 작업

- Chromium Playwright 환경, 실패 시 trace·screenshot 저장, E2E 실행 명령을 추가했다.
- 테스트 전용 일반 직원·SUPER_ADMIN·비밀번호 변경 계정만 환경변수에서 읽도록 구성해
  비밀번호와 토큰을 코드에 기록하지 않도록 했다.
- 기존 직원 46명을 변경하지 않고 E2E 전용 일반 직원·SUPER_ADMIN Auth 계정을 멱등으로
  생성·연결하는 seed와 중복 방지 테스트를 추가했다.
- E2E seed를 다시 실행할 때 테스트 전용 Auth 계정의 비밀번호도 환경변수 값으로 동기화하도록 보완했다.
- 환경변수 템플릿과 README를 DB·서버 전용 Supabase Auth·Frontend 공개 설정·E2E 테스트 설정으로
  구분해 정리하고, 실제 환경 파일의 값은 수정하지 않았다.
- 로그인, 세션 유지, 로그아웃, 직원 권한 범위, 직원 필터, 전자결재 승인,
  비밀번호 변경 후 복구 시나리오를 추가했다.
- 직원 화면의 검색·부서·재직 상태·근무 상태 필터에 접근성 이름을 추가해
  안정적인 E2E selector로 사용할 수 있게 했다.

### 검증 결과

- Playwright Chromium 핵심 E2E 6개 통과: 잘못된 로그인, 로그인·세션 유지·로그아웃,
  일반 직원·관리자 권한 범위, 직원 필터, 전자결재 작성·승인, 비밀번호 변경·복구
- Backend Ruff check / format check 및 전체 pytest 56개 통과
- Frontend lint, TypeScript 검사, production build 통과

## 2026-08-03 · v0.6.3 인증·권한 자동 테스트

### 핵심 작업

- Auth Seed의 계정 연결 동기화 로직을 분리해 같은 직원·Auth 사용자 목록으로 반복 실행해도
  `employee_accounts`가 중복 생성되지 않음을 자동 테스트할 수 있게 했다.
- 비활성 연결 계정과 `employee_accounts` 미연결 Auth 사용자의 보호 API 접근 차단 테스트를 추가했다.
- `TEAM_ADMIN`의 같은 팀 직원 목록 조회 성공, 일반 직원·HR 관리자의 역할 변경 차단,
  지정되지 않은 결재자의 승인 차단 테스트를 추가했다.

### 검증 결과

- Backend Ruff check 및 format check 통과
- Backend 전체 pytest 55개 통과
- Frontend lint 및 TypeScript 검사 통과
- Frontend production build 통과

## 2026-08-02 · v0.6.2 Supabase Auth 인증 검증

### 핵심 작업

- `employee_accounts` migration이 Supabase PostgreSQL에 적용된 것을 확인했다.
- Auth seed를 반복 실행해 직원 46명과 Supabase Auth 계정 46개의 1:1 연결을 확인했다.
- `SUPER_ADMIN` 1명, `HR_ADMIN` 1명, `TEAM_ADMIN` 4명, `EMPLOYEE` 40명의 역할을 확인했다.
- 활성 계정으로 로그인, 잘못된 비밀번호 거부, 보호 경로, 로그아웃, 비밀번호 변경과 원복을 검증했다.
- 비활성 연결 계정과 `employee_accounts` 미연결 Auth 계정의 접근 차단을 검증했다.
- 활성 계정의 access token이 FastAPI `/api/v1/auth/me`에서 올바른 역할로 해석되는 것을 확인했다.
- 공통 Frontend API Client가 Supabase 세션의 access token을 자동으로
  `Authorization: Bearer ...` 헤더에 포함하도록 변경했다.
- `auth/me` 호출의 수동 토큰 전달을 제거해 업무 API와 동일한 공통 인증 경로를 사용한다.
- Backend에 인증 사용자·현재 직원 조회와 `SUPER_ADMIN`, `HR_ADMIN`, `TEAM_ADMIN`,
  직원 관리·결재 권한 dependency를 추가했다.
- 직원 근무 상태와 재직 상태 사유 API를 `AuthenticatedActor` 기반으로 전환해
  `actor_id` query를 인증 주체로 사용하지 않도록 했다.
- 직원 관리 Frontend API에서도 상태 변경 요청의 `actor_id` query 전송을 제거했다.
- 전자결재 제출 API와 Frontend 호출에서 `actor_id`를 제거하고, 제출 이력을
  Bearer token의 현재 직원으로 기록하도록 전환했다.
- 전자결재 승인·반려 API와 Frontend 호출에서도 `actor_id`를 제거하고, 지정 결재자 또는
  `SUPER_ADMIN`만 Bearer token 기준으로 처리하도록 전환했다.
- 전자결재 작성·수정·삭제·목록 API도 Bearer token으로 현재 사용자를 식별하도록 전환하고,
  `author_id`, `actor_id`, `employee_id`를 인증 주체로 받지 않도록 정리했다.
- 채용 요청 작성·상신·포스터 첨부·삭제·채용공고 생성 API도 Bearer token 기준으로 전환하고,
  `requester_id`와 `actor_id`를 인증 주체로 받지 않도록 정리했다.
- 채용 요청·포스터·채용공고 조회 API도 Bearer token 기준으로 전환하고, 포스터 파일은
  공통 API Client가 인증 헤더와 함께 받아 미리보기·다운로드하도록 정리했다.
- 대시보드와 직원 상세 조회에서 사용자 ID query를 제거하고, Supabase JWKS 서명 검증 후
  `employee_accounts` 역할을 조회하도록 인증 경로를 정리했다.
- 직원 생성·수정·비활성화 API를 `SUPER_ADMIN`과 `HR_ADMIN` 권한으로 제한해 일반 직원의
  직원 관리 변경 요청을 차단했다.
- `TEAM_ADMIN`은 같은 팀 직원의 근태·재직 상태 사유만 변경할 수 있고, 다른 팀 변경은
  서버에서 차단하도록 권한 규칙을 보강했다.
- 전자결재 작성자는 관리자 역할이어도 본인 문서를 승인·반려할 수 없게 하고,
  `TEAM_ADMIN`은 같은 팀 작성 문서만 처리하도록 권한 규칙을 보강했다.
- 직원 목록·상세 조회도 역할 범위로 제한해 `SUPER_ADMIN`·`HR_ADMIN`은 전체,
  `TEAM_ADMIN`은 같은 팀, 일반 직원은 본인 정보만 조회하도록 정리했다.
- 직원 역할 변경 API를 추가하고 `SUPER_ADMIN`만 `employee_accounts.role`을 변경할 수 있도록
  제한했다. 일반 직원과 HR 관리자의 역할 변경 요청은 차단한다.
- 역할 변경 시 수행자·대상·변경 전후 역할을 Backend 감사 로그로 기록하도록 추가했다.
- Supabase JWT의 JWKS 검증이 프로젝트 서명 방식과 맞지 않을 때도, Supabase 사용자 조회로
  토큰을 다시 검증해 로그인 세션이 중단되지 않도록 호환 경로를 추가했다.
- 기존 `ADMIN`/`HR_MANAGER` 레거시 판정을 Supabase Auth 역할과 함께 처리하도록 보완했다.
- README에서 완료 전 단계 안내와 중복된 과거 실행 설명을 정리하고, 실제 인증·Bearer JWT·RBAC
  구현 상태 및 1~4단계에서 확인한 로그인·권한·Network·테스트 오류 대응 방법을 Troubleshooting에 반영했다.

### 검증 결과

- Backend Ruff check / format check 통과
- 직원 상태 API의 Bearer 인증 누락 요청 `401` 테스트 통과
- 전자결재 제출 API의 Bearer 인증 누락 요청 `401` 테스트 통과
- 전자결재 승인·반려 API의 Bearer 인증 누락 요청 `401` 테스트 통과
- 전자결재 작성·수정·삭제·목록 API의 Bearer 인증 누락 요청 `401` 테스트 통과
- 채용 요청 변경 API의 Bearer 인증 누락 요청 `401` 테스트 통과
- 채용 요청 조회 API의 Bearer 인증 누락 요청 `401` 테스트 통과
- 대시보드 Bearer 인증 누락 요청 `401` 테스트 통과
- 일반 직원의 직원 관리 변경 요청 `403`, HR 관리자의 직원 수정 요청 성공 테스트 통과
- 역할 변경 API의 `SUPER_ADMIN` 허용·HR 관리자 차단 테스트 통과
- Backend pytest 50개 통과
- Supabase Auth seed 반복 실행 시 중복 없음
- Frontend lint, TypeScript 검사, production build 통과
- 로그인·JWT·권한 오류의 원인과 해결 결과는 [문제 해결 문서](./TROUBLESHOOTING.md)를 참고하세요.

## 2026-08-01 · v0.6.0 직원·조직 관리 안정화

### 핵심 작업

- 46명 직원의 부서·팀·관리자 관계, 검색·필터·상세 API와 조직도 조회를 정리했다.
- 일일 근무 상태, 휴가·근무 사유, 공개 요약과 관리자 전용 메모를 추가했다.
- 조직도 이미지, 상태 정보 아이콘, 직원 관리 화면을 연결했다.
- SQLite fallback을 제거하고 Supabase PostgreSQL 전용 실행 구조로 정리했다.
- 백엔드·프론트엔드 실행 명령을 추가했다.
- Supabase Auth 로그인 화면, 세션 보호, 로그아웃, 비밀번호 변경 화면을 구현했다.

### 검증 결과

- Backend API 테스트 fixture를 Supabase lifespan과 분리했다.
- 직원·조직 관련 Backend pytest와 Frontend lint/build를 확인했다.

## 2026-07-31 · v0.5.x 채용·승인 업무 흐름

### 핵심 작업

- Supabase PostgreSQL 기반 Alembic migration 구조를 정리했다.
- 전자결재 작성·수정·제출·승인·반려와 이력·알림을 구현했다.
- 채용 요청, 결재 연계, 채용 공고, 지원자 단계 흐름을 구현했다.
- 채용 포스터 첨부·미리보기·다운로드와 권한 검사를 추가했다.
- 프론트엔드에서 공통 API client와 업무 화면을 연결했다.

### 검증 결과

- 주요 Backend Ruff·pytest와 migration upgrade/downgrade를 확인했다.
- Frontend lint, TypeScript 검사, production build를 확인했다.

## 2026-07-30 · v0.1~v0.4 기반 기능

### 핵심 작업

- FastAPI·Next.js 기본 구조와 공통 설정을 구성했다.
- 부서·직원·대시보드 조회와 초기 직원·조직 seed를 추가했다.
- 전자결재 문서·이력 모델과 승인 상태 흐름을 구현했다.
- SQLAlchemy 2.0 동기 Session, Repository, Service 계층을 정리했다.
- Supabase PostgreSQL용 초기 Alembic migration과 개발 환경 검증 구조를 추가했다.

### 기록 원칙

- 날짜별로 핵심 기능만 하나의 버전 묶음으로 기록한다.
- 같은 날짜의 세부 작업을 별도 제목으로 반복하지 않는다.
- 검증하지 않은 내용은 완료된 것으로 기록하지 않는다.
